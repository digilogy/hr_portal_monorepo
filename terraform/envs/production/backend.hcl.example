# Copy to backend.hcl and fill in your AWS account ID.
bucket         = "hr-portal-terraform-state-<ACCOUNT_ID>"
key            = "production/terraform.tfstate"
region         = "ap-south-1"
dynamodb_table = "hr-portal-terraform-locks"
encrypt        = true
